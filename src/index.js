import ReactDOM from 'react-dom/client';

import './index.css';
import App from './App';

// this where the app is created and rendered

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
